name: Deploy MkDocs Site

on:
  push:
    branches:
      - main  # Set this to the branch containing your docs

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        persist-credentials: false  # This is necessary for deployments

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'  # Use the Python version you need

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Build the MkDocs site
      run: mkdocs build

    - name: Deploy docs
      uses: mhausenblas/mkdocs-deploy-gh-pages@nomaterial
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        CUSTOM_DOMAIN: jrgochan.github.io
        CONFIG_FILE: ./mkdocs.yml
        # EXTRA_PACKAGES: build-base
        # GITHUB_DOMAIN: github.myenterprise.com
        REQUIREMENTS: ./requirements.txt

    # - name: Deploy to GitHub Pages
    #   uses: peaceiris/actions-gh-pages@v3
    #   with:
    #     github_token: ${{ secrets.GITHUB_TOKEN }}
    #     publish_dir: ./public  # Directory where MkDocs builds the site
